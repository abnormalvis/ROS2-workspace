from setuptools import setup

package_name = 'ignition_sim_pkg'

setup(
    name=package_name,
    version='0.0.0',
    packages=[],
    install_requires=[],
    zip_safe=True,
    author='user',
    description='Ignition/Gazebo demo package with launch, world and bridge config',
    classifiers=['Programming Language :: Python :: 3'],
)
